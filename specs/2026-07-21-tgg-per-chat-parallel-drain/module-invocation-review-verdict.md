# TGG durable consumer module invocation fix — cross-provider review verdict

Reviewed: `ab4b6567b87bf68b6c51e71a98458b46b7f1a5a0` ("fix(tgg): run durable consumer as package module") against its parent `1694932c163d0c72ce3b548e79774904e65f8e3b` (repo: hermes-pcl). 1 file, +1/-1: `deploy/tgg/christopher/systemd/christopher-tgg-hermes.service`.

Context: the first deploy of per-chat concurrency put the updated unit live, then Christopher crash-looped with `ModuleNotFoundError: No module named 'gateway'`. This commit changes only `ExecStart` from absolute script-path invocation (`.../gateway/durable_jsonl_consumer.py run ...`) to package-module invocation (`python -m gateway.durable_jsonl_consumer run ...`), keeping every argument (including `--site-concurrency 4 --chat-batch-size 25`) byte-identical.

## Verdict: CLEAR

### 1. Fix resolves the package import failure — CLEAR
`durable_jsonl_consumer.py` has no top-level `gateway.*` import (top-of-file imports are all stdlib + `yaml`), but it does late-bind three intra-package imports deep inside its execution path: `from gateway.config import load_gateway_config`, `from gateway.replay import ReplayPlan`, `from gateway.run import GatewayRunner` (lines 799-801, 878-880, 1292-1294 — reached only once the consumer actually starts processing, not at parse time, which is why `--help` alone doesn't surface the crash). Absolute path invocation (`python /home/pclaw/.../gateway/durable_jsonl_consumer.py`) sets `sys.path[0]` to the script's own directory (`.../gateway/`) — there is no `gateway` package visible *from inside* `gateway/`, so `import gateway.config` fails exactly as observed in prod. `python -m gateway.durable_jsonl_consumer` instead sets `sys.path[0]` to the process's current working directory, which the unit pins via `WorkingDirectory=/home/pclaw/apps/hermes-pcl` (unchanged by this diff) — `gateway/` is a real subpackage there (`gateway/__init__.py` present), so `gateway.config`/`gateway.replay`/`gateway.run` resolve correctly. Confirmed locally: `.venv/bin/python -m gateway.durable_jsonl_consumer --help` and `... run --help` both exit 0 from the repo root, matching the brief's live read-only control on tgg-app-1.

### 2. No argument/user/environment/gate/pause/service semantics changed — CLEAR
`git show` on this commit touches exactly one line, and only the executable/module target — the interpreter path (`/home/pclaw/apps/hermes-pcl/.venv/bin/python`), the subcommand (`run`), and all ten flags with their values (`--source`, `--cursor`, `--inbox`, `--config`, `--state-db`, `--processing-gate`, `--lock-file`, `--status-file`, `--poll-seconds 2`, `--max-records 100`, `--site-concurrency 4`, `--chat-batch-size 25`) are untouched. `User=`, `Group=`, `SupplementaryGroups=`, `WorkingDirectory=`, every `Environment=`/`EnvironmentFile=` line, `ExecStartPre=` (the engine-slot gate), `Restart=`/`RestartSec=`/`KillMode=`/`KillSignal=`/`TimeoutStopSec=`, and the hardening block (`UMask`, `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem`, `ReadOnlyPaths`) are all outside the diff. Nothing in the demo-pause (`TGG_DEMO_MANAGEMENT_ONLY`) or processing-gate path is touched — those are `EnvironmentFile`/gate-file mechanisms unaffected by how the interpreter locates the entrypoint module.

### 3. Manifest and local invocation/parser checks pass — CLEAR
`build_pa_agent_manifest.py --app-root . --manifest deploy/tgg/christopher/pa-agent.hermes.manifest.json --check` → `{"check": true, "file_count": 567, "ok": true}` (this commit doesn't touch the manifest; confirms no drift). `pcl pa-agent bundle --client tgg --agent christopher --repo . --dry-run` at this commit resolves `source.commit` to `ab4b6567b...` and stages `deploy/tgg/christopher/systemd/christopher-tgg-hermes.service` → `etc/systemd/system/christopher-tgg-hermes.service` with `sha256` matching the on-disk file exactly (`263908b3...`) — the updated `ExecStart` line is what will actually ship. Parser check: `-m gateway.durable_jsonl_consumer run --help` prints the full `run` argument list (`--source`, `--cursor`, `--inbox`, `--config`, `--state-db`, `--processing-gate`, `--lock-file`, `--status-file`, `--poll-seconds`, `--max-records`, `--site-concurrency`, `--chat-batch-size`, `--once`) confirming `argparse` accepts the exact flag set used in `ExecStart` unchanged.

No correctness issues found in the reviewed diff.
