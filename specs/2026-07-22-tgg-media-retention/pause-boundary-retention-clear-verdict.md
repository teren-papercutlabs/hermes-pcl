CROSS-PROVIDER REVIEW VERDICT: CLEAR — b68453fe766df8ba3f4cc8808c2a8cd848ce7d94 (origin/worker/d17d42d8-retention-takeover, hermes-pcl)

Scope: pause-boundary correction on top of merged 19a2c5079. Root defect confirmed real: retention previously ran only inside claimed business lanes, so with TGG_DEMO_MANAGEMENT_ONLY pausing site lanes, fresh site images stayed pending un-retained and retention_total stayed 0. The fix moves retention to an independent bounded pass over pending rows before lane selection, gated business processing on durable retention completion, and keeps the claimed-lane path as an idempotent safety net. Reviewer inspected the full diff, reran the suite, validators, and executed the actual v1->v2 migration against a copy of the LIVE production inbox. No maker code changed by reviewer.

FALSIFIER RESULTS — ALL PASS

1. Live production-shape schema migration from v1: EXECUTED AGAINST A COPY OF THE REAL PROD DB (tgg-app-1 capture-inbox.db, 3,534 rows). Pre-migration columns match the test fixture's v1 DDL exactly (incl. retained_media_count/retention_failures). Result: schema_version 1->2; pending rows: 1,283 images -> retention_state=pending (queued), 344 -> bypassed (incl. every pending video); completed/skipped/failed rows classified but structurally excluded from candidates (status='pending' filter); counters truthful (retention_pending=1283, retention_bypassed=726, held=0); candidates bounded and deterministically ordered. Migration test (test_inbox_v1_migration_classifies_existing_retention_queue) replicates the exact prod DDL.
2. Pending site image retained while pause active: test_demo_pause_retains_pending_site_image_without_runner_or_delivery — row stays business-pending with retention_state=complete, retained file exists, convergence called once, counters and retention_cycle status truthful.
3. Site business row remains pending: same test asserts ("pending", None pa_turn_id, "complete", 1). pending_chat_batches additionally gates business selection on retention_state IN (complete,bypassed).
4. Zero runner/delivery during paused retention: _new_gateway_runner and deliver_management_replies monkeypatched to pytest.fail — never invoked; runner construction is now lazy (only when a lane batch is actually selected).
5. Non-image bypass: staged-at-insert classification (_initial_retention_state) marks non-images bypassed with zero retention I/O (retain_record_media pytest.fail sentinel proves untouched); prod migration bypassed all pending videos.
6. Failed image held/retried without blocking management: test_demo_pause_retention_hold_preserves_management_lane_and_retries — missing-file image goes held with retention_failures=1, management row processes normally, second cycle retries (attempts=2), state=held-pending with truthful retention_hold text.
7. Retention counters/status truthful: retention_total/failures/attempts/pending/complete/bypassed/held all derived from durable rows; retention_cycle summary in status; deployment.yaml statusMediaFields extended and validator enforces the exact list.
8. Bounded batch: retention_candidates LIMIT (default 25), CLI flag, spec field retentionBatchSize=25 enforced by validator; new-rows-before-retries ordering prevents retry starvation of fresh work.
9. No config/allowlist/token/pause regression: zero drift in config.yaml, constitution, patches, runtime-slots, inter_session_ops.md (git name-only across the commit = 4 files: deployment.yaml, validator, consumer, tests). build_runtime_slots.py --check exit 0; validate_deployment_spec.py passes with slot hashes identical to the merged commit.
10. Idempotency: test_demo_pause_retention_rerun_is_counter_and_storage_idempotent — second full run changes nothing (counters, files, convergence call count); record_retention guards transitions (pending/held only) and fail-closes if an outcome cannot be recorded.

Independent verification: 154/154 focused tests at b68453fe7 (149 prior + 5 new); generator check exit 0; deployment validator passes.

NON-BLOCKING NOTES (pre-unpause follow-ups, none affect the paused deployment window)
- N1 SCHEDULER-LOOP STALL UNDER SYSTEMS OUTAGE: the retention pass runs serially inside the scheduler loop before lane dispatch. Each held image whose file exists retries convergence every cycle with a 20s HTTP timeout; with up to 25 held rows and Systems unreachable, new lane dispatch (including management) can stall up to ~8min per cycle until Systems recovers. Missing-file holds fail in microseconds and are harmless. Recommend BEFORE site-lane unpause: background the retention pass or add a per-cycle time budget/backoff for held retries. During the healthy backlog drain (~1,283 images at 25/cycle) expect ~10-25s added dispatch latency per cycle for ~15-20 min, observable via retention_pending.
- N2 SAME-CHAT ORDERING TRADE: a retention-held image row can be leapfrogged by later rows in the same chat (business processes newer messages while the image is held, and the image processes late once cleared). Deliberate and documented in the new ordering contract string; flagged because it weakens the prior strict per-chat FIFO for the held case.
- N3 Migration marks completed/skipped image rows retention_state='pending' (807+718 on prod) — dormant under the status='pending' candidate filter, but they enter scope silently if anyone later widens that query. Historical linkage remains the Systems retro-link tool's job.
- N4 A long-lived hold inflates retention_attempts/failures unboundedly (one per cycle). Truthful but noisy; a backoff would also fix this.

VERDICT: CLEAR for the exact commit b68453fe766df8ba3f4cc8808c2a8cd848ce7d94.
