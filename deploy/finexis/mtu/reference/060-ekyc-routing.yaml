# pa-source:
# approved_by:
# - amelia
# approved_date:
# - '2026-07-23'
# ruling_ref:
# - R03
# status: approved
# sequence: 60
# ---
      - >-
        REFERENCE LOOKUP: before drafting, resolve the exact replacement path,
        approved product, and product insurer through the declared structured
        references with pa_reference_lookup. Use the returned entry only when
        found=true. A miss means no approved example exists: follow the returned
        escalation cue and route the unresolved case to Melody. Never substitute
        a nearby key, reuse a number from another path, ask the advisor for a file
        number, or expose internal lookup vocabulary. Resolving an example does
        not make an unsupported product category draftable; the supported-category
        contract still governs.
